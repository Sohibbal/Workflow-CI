name: Advanced MLOps Pipeline

on:
  push:
    branches:
      - main

jobs:
  mlops:
    runs-on: ubuntu-latest

    env:
      # === IMPORTANT ===
      # Paksa MLflow menyimpan semua run ke folder MLProject/mlruns
      MLFLOW_TRACKING_URI: file:MLProject/mlruns

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12.7'

    - name: Install Dependencies
      run: |
        pip install --upgrade pip
        pip install mlflow[extras]
        pip install scikit-learn pandas numpy

    # ===========================
    # TRAINING MLflow PROJECT
    # ===========================
    - name: Run MLflow Project (Training)
      run: |
        mlflow run MLProject --env-manager=local

    # ===========================
    # GET RUN_ID
    # ===========================
    - name: Get Latest run_id
      id: get_run_id
      run: |
        RUN_ID=$(ls -td MLProject/mlruns/0/*/ | head -1 | xargs basename)
        echo "run_id=$RUN_ID" >> $GITHUB_ENV
        echo "Run ID: $RUN_ID"

    # ===========================
    # UPLOAD ARTIFACT MODEL
    # ===========================
    - name: Upload Model Artifact
      uses: actions/upload-artifact@v4
      with:
        name: trained-ml-model
        path: MLProject/mlruns/0/${{ env.run_id }}/artifacts/model

    # ===========================
    # DOCKER SETUP
    # ===========================
    - name: Setup Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}

    # ===========================
    # BUILD DOCKER IMAGE
    # ===========================
    - name: Build Docker Image from MLflow Model
      run: |
        mlflow models build-docker \
          -m "MLProject/mlruns/0/${{ env.run_id }}/artifacts/model" \
          -n "${{ secrets.DOCKERHUB_USERNAME }}/obesity-model:${{ env.run_id }}"

    - name: Push Docker Image
      run: |
        docker push \
          ${{ secrets.DOCKERHUB_USERNAME }}/obesity-model:${{ env.run_id }}

    - name: Completed
      run: echo "🎯 Pipeline complete!"
